<UserControl x:Class="Osu.Music.UI.Views.PlaylistsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:Osu.Music.Common.Models;assembly=Osu.Music.Common"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Osu.Music.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="10">
            <StackPanel Orientation="Vertical">
                <DockPanel>
                    <TextBlock Text="Playlists" FontFamily="{DynamicResource Roboto}" FontSize="16" FontWeight="DemiBold" Margin="10"/>
                    <Button Style="{DynamicResource PlaylistAction}" Content="&#xe03b;" DockPanel.Dock="Right" HorizontalAlignment="Right" 
                            Command="{Binding ShowCreatePlaylistDialogCommand}"/>
                </DockPanel>

                <ItemsControl ItemsSource="{Binding Playlists}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type m:Playlist}">
                            <TextBlock Text="{Binding Path=Name}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
