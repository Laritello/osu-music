<UserControl x:Class="Osu.Music.UI.Views.DialogCreatePlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Osu.Music.UI.Views"
             mc:Ignorable="d" 
             Width="450" MinWidth="450" MaxWidth="450">
    <Border Style="{DynamicResource DialogBorder}">
        <StackPanel Margin="10 10 10 5">
            <TextBlock Text="Create playlist" FontFamily="{DynamicResource Roboto}" FontSize="16" FontWeight="Bold"/>
            <TextBox Margin="0 10 0 0" Text="{Binding PlaylistName}" FontFamily="{DynamicResource Roboto}" FontSize="16"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 0 0">
                <Button Style="{DynamicResource MaterialButton}" Content="CANCEL" Command="{Binding CancelCommand}"/>
                <Button Style="{DynamicResource MaterialButton}" Content="CREATE" Margin="10 0 0 0" Command="{Binding ConfirmCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
